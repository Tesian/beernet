= form_for [@project, @todo_list], :html => { :class => 'form-horizontal' } do |f|
  .control_group
    = f.label "Nom"
    .controls
      = f.text_field :name
  .form-actions
    = f.submit nil, :class => "btn btn-primary"
